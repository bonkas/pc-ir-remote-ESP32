# ESPHome secrets — never commit these
firmware/esphome/secrets.yaml
.esphome/

# KiCad source files — not published, only gerbers and BOM are committed
hardware/*/kicad/

# Private project notes — not published
CONTEXT.md

# OS files
.DS_Store
Thumbs.db
desktop.ini
